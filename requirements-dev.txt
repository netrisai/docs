# Local docs-authoring environment (NOT used by the CI build).
#
# This pins the same Python/Sphinx/Esbonio environment already in use locally
# (captured from a working .venv on 2026-07-06) so a new technical writer can
# reproduce it exactly for local RST preview and linting in VS Code (Esbonio).
#
# The public site build itself does NOT use this file -- .github/workflows/main.yml
# builds via the private `netrisai/docs-tooling` GitHub Action, which manages its
# own environment. This file is purely for local authoring/preview.
#
# Setup:
#   python3.11 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements-dev.txt
#
# See the "Local RST Authoring Environment (VS Code + Esbonio)" page in Confluence
# (Documentation Procedures section) for the full setup walkthrough.

# --- Sphinx + theme + extensions actually used by conf.py ---
Sphinx==8.2.3
sphinx-rtd-theme==3.0.2
sphinx-copybutton==0.5.2
sphinx-design==0.7.0
sphinxcontrib-googleanalytics==0.5
sphinx-collapse==0.1.4
sphinx-tabs==3.5.0
# ^ installed and available, but currently commented out of conf.py's `extensions` list

# --- Local RST preview / language server for VS Code ---
esbonio==2.1.0

# --- RST linters, used by the VS Code "reStructuredText" extension's
#     restructuredtext.linter.doc8.* / restructuredtext.linter.rstcheck.* settings ---
doc8==2.0.0
rstcheck==6.2.5

# --- Transitive dependencies, pinned for an exact reproduction ---
aiosqlite==0.22.1
alabaster==1.0.0
attrs==26.1.0
Babel==2.18.0
cattrs==26.1.0
certifi==2026.6.17
charset-normalizer==3.4.8
docutils==0.21.2
gitdb==4.0.12
GitPython==3.1.50
idna==3.18
imagesize==2.0.0
Jinja2==3.1.6
lsprotocol==2025.0.0
MarkupSafe==3.0.3
packaging==26.2
platformdirs==4.10.0
pygls==2.1.1
Pygments==2.20.0
requests==2.34.2
roman-numerals-py==4.1.0
smmap==5.0.3
snowballstemmer==3.1.1
sphinxcontrib-applehelp==2.0.0
sphinxcontrib-devhelp==2.0.0
sphinxcontrib-htmlhelp==2.1.0
sphinxcontrib-jquery==4.1
sphinxcontrib-jsmath==1.0.1
sphinxcontrib-qthelp==2.0.0
sphinxcontrib-serializinghtml==2.0.0
typing_extensions==4.16.0
urllib3==2.7.0
websockets==16.0
annotated-doc==0.0.4
annotated-types==0.7.0
markdown-it-py==4.2.0
mdurl==0.1.2
pydantic==2.13.4
pydantic_core==2.46.4
restructuredtext_lint==2.0.2
rich==15.0.0
rstcheck-core==1.3.0
shellingham==1.5.4
stevedore==5.8.0
tomli==2.4.1
typer==0.26.8
typing-inspection==0.4.2
